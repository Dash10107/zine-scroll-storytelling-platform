"use client"

import { motion } from "framer-motion"

export function LetterboxOverlay() {
  return (
    <>
      {/* <motion.div
        className="fixed top-0 left-0 right-0 z-40 bg-black pointer-events-none"
        initial={{ height: "100vh" }}
        animate={{ height: "3rem" }}
        transition={{
          duration: 1.2,
          ease: [0.16, 1, 0.3, 1],
          delay: 0.3,
        }}
      />
      <motion.div
        className="fixed bottom-0 left-0 right-0 z-40 bg-black pointer-events-none"
        initial={{ height: "100vh" }}
        animate={{ height: "3rem" }}
        transition={{
          duration: 1.2,
          ease: [0.16, 1, 0.3, 1],
          delay: 0.3,
        }}
      /> */}
    </>
  )
}
